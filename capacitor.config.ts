import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'suprema-biostar-app.mobile',
  webDir: 'www'
};

export default config;
